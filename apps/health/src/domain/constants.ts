/**
 * localStorage keys owned by health stores. Centralised here so nothing
 * else in the app spells them out — change a key here and grep finds
 * every consumer.
 */

export const HEALTH_PROFILE_KEY = "health_profile";
export const HEALTH_CHECKINS_KEY = "health_checkins";
export const HEALTH_EXERCISES_KEY = "health_exercises";
export const HEALTH_SESSIONS_KEY = "health_sessions";
